export const runtimeConfig = {
  sessionCookieName: "app_shell_session",
  loginPath: "/login",
  afterLoginPath: "/dashboard",
  publicPaths: ["/", "/login", "/signup", "/api/auth", "/api/webhooks/stripe", "/_next", "/favicon.ico"]
}
